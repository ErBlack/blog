<!DOCTYPE html>
<html lang="ru">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="stylesheet" href="/blog/main.css" />
        {% for srtle in styles %}
        <link rel="stylesheet" href="{{srtle}}" />
        {% endfor %}
        <title>{{title}}</title>
    </head>
    <body>
        {{content}}
    </body>
</html>
